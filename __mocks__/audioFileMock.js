/**
 * Mock for audio file imports (wav, mp3, ogg, m4a, aac)
 *
 * In React Native/Expo, audio files are imported as numeric asset IDs.
 * This mock returns a numeric value to simulate that behavior in tests.
 */
module.exports = 1;
